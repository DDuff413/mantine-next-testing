import { createStyles } from '@mantine/core';

export default createStyles(() => ({
  card: {
    display: 'flex',
    flexDirection: 'column',
    gap: 20,
    width: 328,
  },
  level: {},
  description: {},
  example: {},
}));
